# H618 AI电视盒子BSP完整文档

> Note created at `/home/lijian/project/Obsidian/H618 AI电视盒子BSP完整文档.md` (15198 bytes).

The note covers all 6 source files and includes:
- YAML frontmatter with 14 tags, category, dates, status
- Detailed project overview of the H618 BSP
- Full technology stack table (SoC through app layer)
- Architecture with directory layout, boot flow, disk layout, device tree strategy
- 10 core knowledge sections with code snippets (ATF, kernel patches, boot.cmd, rootfs build, systemd in chroot, Docker kernel config, VPU/V4L2, USB camera, netplan, APT format changes)
- Complete build steps (env setup, one-shot, per-stage, SD-to-eMMC, post-flash, benchmarks)
- 7 key learning takeaways
- 8 related note links and 8 reference URLs
