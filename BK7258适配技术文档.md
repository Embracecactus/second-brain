# BK7258 芯片 open-vela 适配技术文档

> 面向初级开发者的结构化技术文档 · 可作为面试准备材料
> 范围：仅基于 `contest2026_135_yongwangzhiqian/` 仓库（不含 `qemu-bk7258/` 与 `nuttx/.worktrees/` 中间产物）
> 配套源码路径前缀统一为：`/home/lijian/project/open-vela/contest2026_135_yongwangzhiqian/`

---

## 0. 阅读导航（TL;DR）

BK7258 是博通集成（Beken）出品的 **Wi-Fi 6 + BLE 5.4 Combo SoC**，采用 **3 核 ARM Cortex-M33** 的非对称多核（AMP）架构。本项目把 openvela / NuttX RTOS 移植到该芯片（涂鸦 T5-AI 模组），参赛 2026 首届 openvela AI 硬件开发者大赛。

适配工作可拆成三条主线，本文按此组织：

| 章节 | 主题 | 核心目录 |
|---|---|---|
| 1 | 项目概述与芯片背景 | `README.md`、`docs/bk7258-t5ai/` |
| 2 | Bootloader 适配（BL1 → BL2 → 应用） | `board/bk7258/bootloader/`、`board/bk7258/partitions/` |
| 3 | 芯片级适配与驱动适配 | `board/bk7258/chip/`、`board/bk7258/bk_idk/` |
| 4 | 关键文件与目录结构梳理 | 全仓 |
| 5 | 构建、烧录、调试流程 | `tools/bk7258/`、`docs/bk7258-t5ai/nuttx-port/` |

**一句话架构**：`BootROM(芯片固化) → 自研 Tier-1 BL1(板级) → MCUboot BL2(双副本) → NuttX(CP=CPU0 单核 + AP=CPU1/2 双核)`，CP/AP 通过官方 ARMINO SDK 静态库 + NuttX wrapper 衔接，核间用 RPTUN/RPMsg 通信。

---

## 1. 项目概述与 BK7258 芯片背景

### 1.1 项目目标

本仓是参赛队的**专属代码仓**，仅承载 team overlay（板级适配 + 自定义驱动 + 自研 bootloader），通过 repo manifest 的 `<linkfile>` 软链进外层 openvela 编译树（`nuttx/`、`apps/` 等公共仓库**零改动**）。

- **软件栈自上而下**：
  ```
  BootROM(芯片固化)
    → 自研 Tier-1 BL1(板级, XIP @0x02000000)
    → MCUboot BL2(双副本 Primary/Secondary @0x024d0000 / 0x024f0000)
    → NuttX(CP=CPU0 单核 UP + AP=CPU1/CPU2 双核 SMP)
  ```
- **衔接方式**：CP/AP 通过官方 ARMINO SDK 静态库（`armino_as_lib`）+ NuttX wrapper 衔接；核间通信用 NuttX RPTUN/OpenAMP/RPMsg。
- **约束**：所有官方 NuttX / apps / SDK 源码与 SDK 静态库保持**只读**，适配实现一律落在 `contest2026_135_yongwangzhiqian/` 内。

### 1.2 BK7258 芯片规格

> 事实来源：`docs/bk7258-t5ai/README.md`、`docs/bk7258-t5ai/hardware/t5ai-core-v101-hardware-map.md`、`docs/bk7258-t5ai/nuttx-port/bk7258-build-flash-debug-sop.md`。

| 项 | 内容 | 说明 / 依据 |
|---|---|---|
| 厂商 / 模组 | Beken（博通集成）/ 涂鸦 T5-AI（T5AI-Core V1.0.1） | `docs/bk7258-t5ai/README.md` |
| 架构 | ARM Cortex-M33（STAR r1p0） | J-Link 探测 `core: STAR r1p0` |
| 多核 | **3 核 M33**：CPU0=CP（控制面，NuttX UP）；CPU1/CPU2=AP（应用面，NuttX SMP，映射为 AP logical CPU0/CPU1） | `docs/.../README.md`、jlink-swd-debug-guide §6.1 |
| 主频 | DPLL 最高 ~480 MHz（曾验证）；**当前产品路径采用已验证的 320 MHz 运行时 DVFS** | `README.md` N4 段 |
| Flash | 片上 Flash，逻辑基址 `0x02000000`，空间到 `0x800000` ⇒ **约 8 MiB**；校准尾区 `0x7fa000..0x800000` 不可动 | `board/bk7258/bootloader/README.md` |
| PSRAM | 外接 **16 MiB**（APS128XXO，id=0x8d08） | `README.md` N14 段 |
| 外设大类 | Wi-Fi 6、BLE 5.4、GPIO、UART、I2C、SPI、I2S、SDIO、QSPI、PWM、CAN、DVP、LCD、USB Host/CDC、Ethernet、RTC、Timer、WDT、TRNG、SARADC、温度、音频（MIC/DAC）、mailbox（核间） | `board/bk7258/chip/ap/*.c`、`chip/cp/*.c` |
| 调试接口 | SWD（P20/P21 = SWCLK/SWDIO，引到 J2） | `hardware/t5ai-core-v101-hardware-map.md` §4 |

> ⚠️ **未确认项（给面试/文档留白）**：
> - 片上 SRAM 总字节数：文档只给地址空间（ITCM `0x00000000`、DTCM `0x20000000`、PSRAM `0x60000000`），未给出单一明确容量，需查官方 SDK `reg_base.h` 或芯片手册。
> - 主频应表述为"最高可达 ~480 MHz（DPLL），当前验证 320 MHz"，不要写死单一数字。

### 1.3 多核（AMP）模型要点

- **CP（CPU0）**：常驻独立 NuttX UP 内核，负责系统启动、看门狗、电源管理协调（`pm_coord_cp`）、蓝牙/Wi-Fi 控制器服务端、AP 监管（`ap_supervisor`）、跨核 mailbox 服务端。
- **AP（CPU1/2）**：运行应用协议栈与大部分外设驱动，可 SMP（仅 `CONFIG_BK7258_AP_SMP_BOOTSTRAP`）。
- **启动由 CP 释放 AP**：CP reset 关闭 boot 遗留 WDT → 经 sys 寄存器 `sys_drv_set_cpu1_boot_address_offset` + reset 释放 AP。AP **不在 bootloader 链路里**。

---

## 2. Bootloader 适配流程（BL1 + BL2）

### 2.1 启动链总览

```
[上电]
  │  BootROM(厂商二进制,不在仓库):读物理 0x0 的 CRC 包,校验
  │    逻辑 0x100 处 magic "BK7236\x10\x00",加载 BL1 向量表(XIP 0x02000000)
  ▼
[BL1 - Tier-1, XIP 0x02000000, 源码完整]
  │  start.S → boot_main.c → 选 BL2 或直接跳 CP
  ▼ (BL1_USE_BL2=1 时) 或 直接 ▼(=0 时跳 CP)
[BL2 - 上游 MCUboot 裸机外壳, SRAM 0x28020000]
  │  验签 CP+AP → 选主/备对 → 跳 CP
  ▼
[CP NuttX - XIP 0x02010000] → CP 释放 AP [XIP 0x02150000]
```

### 2.2 BL1（Tier-1 自研 bootloader）

BL1 有完整源码，既是直接启动 CP 的 bootloader，也是把 BL2 拷进 SRAM 的加载器。

**关键源文件**（完整路径 `.../contest2026_135_yongwangzhiqian/board/bk7258/bootloader/`）：

| 文件 | 职责 |
|---|---|
| `start.S` | 汇编 trampoline、向量表、BL1→(App/BL2) 硬化跳转 |
| `boot_main.c` | C 主函数 `c_main()`（时钟/校验/分区解析/BL2 拷贝/策略发布） |
| `boot_clock.c` | 冷启动 DPLL 上电 + 120 MHz 交付 |
| `boot_flash.c` | 只读原始 Flash 访问（manifest 读取） |
| `boot_runtime.c` | cache/MPU/次级核归一化（clean-room 复刻官方序列） |
| `boot_bl1_manifest.c/.h` | BL1 Manifest 解析/校验 |
| `boot_bl1_manifest_key.c` | 开发根公钥（仅公钥） |
| `boot_bl1_policy.c/.h`、`boot_bl1_boot_flag_core.c/.h`、`boot_bl1_handoff_core.c/.h` | 槽序/版本策略/向量校验 |
| `bk7258_bl1_pack.py` | BL1 打包器（control/manifest/crc） |
| `Makefile` | BL1 构建（`make` → bl.elf/bl.bin/bl_crc.bin） |
| `bootloader.ld` | BL1 链接脚本（FLASH @0x02000000，slot 0x10000 = 64 KiB） |

**BL1 启动流程（`boot_main.c`）**：
1. `start.S` Reset_Handler：关中断、设 MSPLIM=0x2802F800、I-cache 失效（防 downloader/SYSRESETREQ 残留）、早期 SoC/Flash/SYS 修复（保持 CPU1/2 复位）、使能 SWD、禁 WDT、配置 UART（默认 UART1/460800）。
2. `c_main()`：WDT init/feed → `boot_clock_cold_init()`（DPLL 上电 + 标定，交付 120 MHz）→ 解析 FAL 分区表（4 项）→ 分支：
   - `BL1_USE_BL2=0`（默认）：校验 `cp_app` 头部 → 返回 0x02010000 直接跳 CP。
   - `BL1_USE_BL2=1`：找到 `bl2` 分区（XIP 0x024d0000）→ 校验 BL2 向量/MSP/reset 落在 SRAM 窗 → 拷 0x3000 字节到 SRAM 0x28020000 → 发布策略到 `0x2801ffd0`（首选/回退槽）→ 返回 0x28020000。

关键片段（`boot_main.c:506-680`，路径选择）：
```c
#if !BK7258_BL1_USE_BL2
    app = fal_find("cp_app");
    ... validate_direct_app(app_vec, app->len);
    return app_vec;                 /* 直接返回 0x02010000 */
#else
    ... fal_find("bl2");
    /* 校验 BL2 向量 + 拷 BL2 到 SRAM(0x28020000) + 发布策略 */
    boot_bl2_policy_store(&bl2_policy);    /* 写 0x2801ffd0 */
    return BL2_RAM_BASE;                   /* 返回 0x28020000 */
#endif
```

> 📌 **冷复位契约**：多核冷启动不止"读 header + branch"，必须清理 cache/MPU、WDT、次级核 power、UART ownership、中断状态，否则不可重复冷复位（`boot_runtime.c` + `start.S`）。这是新人最易踩的坑。

### 2.3 BL2（MCUboot 裸机外壳）

BL2 基于 **NuttX 所携带的上游 MCUboot 的 `bootutil`**，仓库内只提供"裸机外壳 + 板级适配"，运行在 **SRAM `0x28020000`**（不直接 XIP）。

**关键源文件**（`.../contest2026_135_yongwangzhiqian/board/bk7258/bootloader/bl2/`）：

| 文件 | 职责 |
|---|---|
| `bk7258_bl2_main.c` | 主流程（平台初始化、pair 校验、跳转） |
| `bk7258_bl2_flash_map.c` | MCUboot `flash_area_*` 板级后端 |
| `bk7258_bl2_keys.c` | 验签公钥（ECDSA-P256） |
| `bk7258_bl2_security_cnt.c` | 安全计数器 |
| `bk7258_bl2_mcuboot_{asn1,boot,ecc,ecc_dsa,ecdsa,keys}.c` | MCUboot 板级胶水 |
| `bl2_start.S` | BL2 入口（MSPLIM、清 bss、跳 main） |
| `bl2.ld` | BL2 链接脚本（纯 SRAM） |
| `include/mcuboot_config/mcuboot_config.h` | MCUboot 板级配置 |
| `include/sysflash/sysflash.h`、`flash_map_backend/` | 分区/flash 后端头 |
| `Makefile` | BL2 构建 |

**MCUboot 配置**（`bl2/include/mcuboot_config/mcuboot_config.h:5-14`）：
```c
#define MCUBOOT_SIGN_EC256            /* ECDSA-P256 签名 */
#define MCUBOOT_USE_TINYCRYPT         /* 用 TinyCrypt 验签 */
#define MCUBOOT_DIRECT_XIP            /* 直接 XIP,无需 swap */
#define MCUBOOT_VALIDATE_PRIMARY_SLOT
#define MCUBOOT_USE_FLASH_AREA_GET_SECTORS
#define MCUBOOT_MAX_IMG_SECTORS 1
#define MCUBOOT_IMAGE_NUMBER 2        /* CP + AP 两镜像视为一个启动对 */
#define MCUBOOT_HW_ROLLBACK_PROT      /* 硬件回滚保护 */
#define MCUBOOT_WATCHDOG_FEED() do { } while (0)
```

**BL2 主流程**（`bk7258_bl2_main.c`）：
1. `bk7258_bl2_platform_init()`（L183-194）：WDT 接管、VTOR=SRAM、禁 NVIC、清 I-cache。
   ```c
   static void bk7258_bl2_platform_init(void) {
     boot_wdt_init();
     REG32(SCB_VTOR) = BK7258_BL2_RAM_BASE;  /* 0x28020000 */
     REG32(SYSTICK_CTRL) = 0;
     REG32(NVIC_ICER0) = 0xffffffffu;        /* 清 NVIC */
     REG32(NVIC_ICPR0) = 0xffffffffu;
     REG32(SCB_ICIALLU) = 0;                 /* I-cache 失效 */
     __asm volatile ("dsb sy; isb" ::: "memory");
   }
   ```
2. `bk7258_bl2_load_boot_policy()`：从 `0x2801ffd0` 读取 BL1 发布的（首选/回退）槽策略。
3. `bk7258_bl2_try_pair(preferred)`：调用上游 `FIH_CALL(boot_go, …)` 验签 CP+AP（ECDSA-P256/TinyCrypt）→ 校验 CP/AP 同代（版本+安全计数）→ 校验 AP 向量 → 失败且设了 fallback 则再试 secondary。
4. [若选 secondary] `bk7258_bl2_remap_secondary()`：Flash remap 把 B 对映射到 A 窗口。
5. `boot_prepare_app_handoff()`（cache/MPU 清理）→ `bk7258_bl2_jump()`：VTOR=CP 向量，MSPLIM 恢复 0x28010000，`bx` CP reset。

**跳转到 CP 的硬化 epilogue**（`bk7258_bl2_main.c:481-511`）：
```c
  __asm volatile ("cpsid i" ::: "memory");
  REG32(SYSTICK_CTRL) = 0;
  REG32(NVIC_ICER0) = 0xffffffffu; ...
  REG32(SCB_VTOR) = image;             /* 指向 CP 向量表 */
  __asm volatile ("msr msplim, %0; dsb sy; isb" :: "r"(BK7258_CP_MSPLIM) : "memory");
  bk7258_bl2_enter(msp, reset);        /* 清 r0-r12, bx r9(reset) */
```
跳转前校验：`msp` 落在 `0x28010000..0x28050000`、`reset` 带 Thumb 位、且 `reset_addr` 在 `SLOT_A_CP_XIP_START(0x02010000)..SLOT_A_AP_XIP_START(0x02150000)`。

### 2.4 安全启动 / 签名

- **MCUboot 侧**：ECDSA-P256 + TinyCrypt + 硬件回滚保护。公钥 `ecdsa_pub_key[]` 在 `bl2/bk7258_bl2_keys.c:4-24`，用于校验 CP/AP 镜像（imgtool 签名）。
- **BL1 板级 Manifest**：开发根公钥在 `boot_bl1_manifest_key.c`（仅公钥）。打包时 `bk7258_bl1_pack.py:265-270` 强制校验软件根哈希——**这是软件开发根，不是厂商 BootROM/OTP 根**。
- ⚠️ **未真正启用 OTP/eFuse 安全启动**：`BL1_MANIFEST_ENFORCE` 默认 0；板为 CM（空 hash）时回退到软件开发根。Manifest 仅是可恢复的开发授权层。

### 2.5 Flash 分区布局

来源：`board/bk7258/partitions/bk7258/auto_partitions.csv`（活跃）+ 生成头 `board/bk7258/include/bk7258_partition_layout.h`。布局 ID `bk7258-v3119-ab-124ebfab37ca1fcd`。

| 分区名 | 物理偏移 | 大小 | 逻辑偏移 | XIP 地址 | 用途 / Role |
|---|---|---|---|---|---|
| primary_bootloader | 0x000000 | 0x11000 (68K) | 0x000000 | 0x02000000 | **BL1 自身**(boot) |
| primary_cp_app | 0x011000 | 0x154000 (1360K) | 0x010000 | 0x02010000 | **CP NuttX slot A**(slot_a_cp) |
| primary_ap_app | 0x165000 | 0x121000 (1156K) | 0x015000 | 0x02150000 | **AP NuttX slot A**(slot_a_ap) |
| s_app | 0x286000 | 0x275000 (2516K) | — | —(raw) | **slot B 对暂存**(slot_b_pair, data) |
| usr_config | 0x4fc000 | 0x0e000 (56K) | — | — | 厂商配置(vendor_config) |
| primary_manifest | 0x50b000 | 0x01000 (4K) | — | — | BL1 primary Manifest |
| secondary_manifest | 0x50c000 | 0x01000 (4K) | — | — | BL1 secondary Manifest |
| **bl2** | **0x51d000** | **0x22000 (136K)** | **0x4d0000** | **0x024d0000** | **NuttX 自有 BL2**(bl2, code) |
| littlefs | 0x600000 | 0x100000 (1M) | — | — | 文件系统(littlefs) |
| easyflash | 0x7fa000 | 0x02000 | — | — | easyflash_cp（校准/数据） |
| easyflash_ap | 0x7fc000 | 0x02000 | — | — | easyflash_ap |
| sys_rf | 0x7fe000 | 0x01000 | — | — | 射频校准(calibration_rf) |
| sys_net | 0x7ff000 | 0x01000 | — | — | 网络校准(calibration_net) |

> BL2 次槽由其后 pre-LittleFS 间隙推导（raw 0x53f000、XIP 0x024f0000）；暂存 profile `secureboot_xip_cp_ap.csv` 则显式命名 primary/secondary 两个 136K 行。

### 2.6 关键地址速查

| 项 | 地址 |
|---|---|
| BL1 XIP | `0x02000000`（栈顶 MSP `0x2809F700`） |
| CP slot A XIP | `0x02010000` |
| AP slot A XIP | `0x02150000` |
| BL2 primary XIP | `0x024d0000`；BL2 secondary XIP `0x024f0000` |
| BL2 SRAM 执行窗 | `0x28020000..0x2802ffff`（128K）；BL2 向量 MSP `0x28040000` |
| CP RAM 下界 | `0x28010000`（MSPLIM 交接值） |
| AP 向量 RAM | `0x28050000..0x2809f000` |
| BL1→BL2 策略记录 | `0x2801ffd0` |

### 2.7 两条"BL2"构建路径说明（给面试者提醒）

1. `board/bk7258/bootloader/bl2/` —— **裸机 MCUboot 外壳**，由 `bl2/Makefile` 编译，包装上游 `bootutil`，链接到 SRAM `0x28020000`，由 BL1 加载。**以这条为主。**
2. `board/bk7258/configs/bl2_mcuboot/defconfig` —— NuttX 内部的 MCUboot 配置（`CONFIG_BOOT_MCUBOOT=y`、`mcuboot_loader_main`），把 MCUboot 当作独立 NuttX bootloader 镜像构建（NuttX 侧 imgtool/MCUBoot 镜像配置参考）。

> `bootloader/README.md` 声明旧的 `*_mcuboot` profile 名已 "SUPERSEDED / NON-RUNNABLE"。实际烧录用 `bl2/` 裸机路径。

---

## 3. 驱动适配部分（芯片级 + 外设驱动）

### 3.1 芯片适配层目录结构

```
board/bk7258/
├── chip/                      # 芯片适配层(核心)
│   ├── chip.h                 # NuttX 自定义芯片头桥接
│   ├── Kconfig                # 芯片级 Kconfig(所有 BK7258_* 配置)
│   ├── Make.defs / CMakeLists.txt
│   ├── ap/                    # AP 核独有(CPU1/CPU2)驱动
│   ├── cp/                    # CP 核独有(CPU0)驱动
│   ├── common/                # CP/AP 共享代码(同一份源码被两核构建)
│   └── include/               # 芯片公共头(irq.h / memorymap.h / 各外设 .h / mcuboot_config)
├── bk_idk/                    # armino SDK 作为 lib 的集成
├── src/                       # 板级平台代码(CP 注册在此: bk7258_platform.c)
└── boards/t5ai_core/          # 具体板型 bringup(include + src)
```

**CP / AP / common 划分**：
- **common**：`serial/irq/timerisr/allocateheap/psram/idle/sdk_irq/sdk_runtime/sdk_stubs/os_adapt/rptun/rptun_mbox/rpmsg_*`。通过 `CONFIG_BK7258_AP_CORE` 宏在编译期区分角色（`sdk-bundles.cmake:20-24` 据该宏选 `ap`/`cp` bundle）。
- **CP 侧驱动**：`wdt`、`gpio_lowerhalf`、`saradc_server`。
- **AP 侧驱动**：`i2c/spi/pwm/saradc/sdmadc/gpioe/rtc/timer/i2s/dmic` 及媒体/显示/无线等。

### 3.2 armino SDK 作为 lib 的集成方式

集成文档：`board/bk7258/bk_idk/README.md`。

- **布局**：`bk_idk/armino_as_lib/versions/{legacy, v3.1.1.9, v3.1.1.9-sdio4}/{cp,ap}/{include,config,libs}`。
  - `legacy`：迁移前保留，**禁止用于构建/验证**。
  - `v3.1.1.9`：**当前默认版本**（CP+AP）。
  - `v3.1.1.9-sdio4`：**仅 AP**，四线 SDIO 变体。
- **lib 文件**：预编译 `.a`（如 `v3.1.1.9/cp/libs/libdriver.a`、`libbk7258.a`、`libbk_pm.a`、`libwifi.a` 等），以 SHA-256 + provenance 证据方式被仓库跟踪（`manifests/<version>/<role>.{sha256,provenance}`），校验 fail-closed。
- **头文件路径**：驱动 `#include <driver/i2c.h>`、`<driver/gpio.h>`、`<driver/pwm.h>` 等，由构建系统把 `armino_as_lib/versions/<ver>/<role>/include` 加入 `-I`（`sdk-bundles.cmake:67-77` 校验三目录存在，否则 FATAL_ERROR）。
- **版本/角色选择**（`sdk-bundles.cmake:10-81`）：
  ```cmake
  set(BK7258_SDK_SUPPORTED_BUNDLE_VERSIONS legacy v3.1.1.9 v3.1.1.9-sdio4)
  # 默认 v3.1.1.9;可用环境变量 BK7258_SDK_BUNDLE_VERSION / BK7258_AP_SDK_BUNDLE_VERSION / BK7258_CP_SDK_BUNDLE_VERSION 覆盖
  if(CONFIG_BK7258_AP_CORE) set(BK7258_SDK_ROLE ap) else set(cp)
  set(BK7258_SDK_ROLE_DIR ".../versions/${VER}/${ROLE}")
  ```
  未知版本立即 FATAL_ERROR；`v3.1.1.9-sdio4` 仅限 AP。
- **静态链接**：NuttX 芯片侧 `.c` 只调用 SDK 符号，最终与对应 `role/libs/*.a` 链接。注意：`libdriver.a` 在 AP 侧含 `bk_i2c_*`/`bk_pwm_*` 等实现，CP bundle 仅含头文件 ⇒ `i2c.c`/`pwm.c` 等被 `CONFIG_BK7258_AP_CORE` 守卫。
- **SDK IPC runtime**：AP 驱动先调用 `bk7258_sdk_runtime_initialize()`（peripherals.c:280）建立系统寄存器与 mailbox 服务，之后才能发同步 SDK 请求（`CONFIG_BK7258_SDK_IPC_RUNTIME`）。

### 3.3 驱动适配清单

> 路径均为 `.../contest2026_135_yongwangzhiqian/board/bk7258/chip/` 下。

| 驱动名 | 文件 | 适配外设 | 关键 NuttX 接口 | 核 |
|---|---|---|---|---|
| I2C master | `ap/bk7258_i2c.c` | I2C 主机 | `i2c_master_s` / `i2c_ops_s`；`i2c_register()`→`/dev/i2cN` | AP |
| SPI master | `ap/bk7258_spi.c` | SPI 主机 | `spi_dev_s` / `spi_ops_s`；`spi_register()`→`/dev/spiN` | AP |
| PWM | `ap/bk7258_pwm.c` | PWM 通道 | `pwm_lowerhalf_s` / `pwm_ops_s`；`pwm_register()`→`/dev/pwmN` | AP |
| SARADC | `ap/bk7258_saradc.c` | 逐次逼近 ADC | `adc_dev_s` / `adc_ops_s`；`adc_register()` | AP |
| SDMADC | `ap/bk7258_sdmadc.c` | Sigma-Delta ADC | `adc_dev_s` / `adc_ops_s`；`adc_register()` | AP |
| GPIOE (ioexpander) | `ap/bk7258_gpioe.c` | 多引脚 GPIO | `ioexpander_dev_s` / `ioexpander_ops_s` | AP |
| RTC | `ap/bk7258_rtc.c` | AON RTC | `rtc_lowerhalf_s` / `rtc_ops_s`；`up_rtc_initialize()` | AP |
| Timer | `ap/bk7258_timer.c` | 通用定时器 | `timer_lowerhalf_s` / `timer_ops_s`；`timer_register()` | AP |
| I2S | `ap/bk7258_i2s.c` | I2S 音频 | `i2s_dev_s` / `i2s_ops_s`；`i2schar_register()` | AP |
| DMIC | `ap/bk7258_dmic.c` | 数字麦克风 | 辅助 helper（`bk7258_dmic_initialize()`），不独立注册字符设备 | AP |
| WDT | `cp/bk7258_wdt.c` | APB 硬件看门狗 | `watchdog_lowerhalf_s` / `watchdog_ops_s`；`watchdog_register()`→`/dev/watchdog0` | CP |
| GPIO lower-half | `cp/bk7258_gpio_lowerhalf.c` | 板级 LED/KEY | `gpio_dev_s` / `gpio_operations_s`；`gpio_pin_register()`→`/dev/gpioN` | CP |
| SARADC server | `cp/bk7258_saradc_server.c` | CP ADC mailbox 服务端 | 启动 SDK `bk_adc_driver_init()` 供 AP 客户端 | CP |

补充（同属适配层，位于 `chip/ap/`）：`can.c`、`mic.c`/`aud.c`、`usbhost.c`、`sdio.c`、`qspi.c`、`trng.c`、`lcd_*.c`/`dvp.c`/`jpeg_*.c`/`scale_rotate.c`、`wifi.c`、`bt_hci.c`、`ble_gatt.c`、`lin.c`、`slcd.c` 等；CP 另有 `touch.c`、`temperature_server.c`、`bt_controller.c`、`pm_coord_cp.c`、`ap_supervisor.c` 等。

### 3.4 适配方式（核心范式）

> **所有外设驱动都是"零寄存器访问"的 NuttX lower-half 封装**：NuttX 侧结构体（`i2c_ops_s` 等）的方法函数体内调用 armino SDK 的 `bk_xxx_*` HAL，**不直接操作芯片寄存器**。寄存器操作全部在预编译 SDK bundle 内完成。

以两个典型驱动说明：

#### 3.4.1 I2C（AP）— `chip/ap/bk7258_i2c.c`

- 注册只发布 `/dev/i2cN`，不初始硬件；上层首次 `open()` 触发 `setup()`。
- `bk7258_i2c_setup()`（L405）：首次调用 `bk_i2c_driver_init()`（全局一次性）+ `bk_i2c_init(id, &cfg)`。
- `bk7258_i2c_transfer()`（L209）：遍历 `i2c_msg_s[]`，组合事务→`bk_i2c_memory_read/write()`，单段→`bk_i2c_master_read/write()`；不支持 10 位地址与单独 NOSTOP/NOSTART（返回 `-ENOTSUP`）。

```c
// chip/ap/bk7258_i2c.c:113-129
static const struct i2c_ops_s g_bk7258_i2c_ops =
{
  .transfer  = bk7258_i2c_transfer,
  .setup     = bk7258_i2c_setup,
  .shutdown  = bk7258_i2c_shutdown,
};
static struct bk7258_i2c_priv_s g_bk7258_i2c =
{
  .dev.ops  = &g_bk7258_i2c_ops,
  .id       = (i2c_id_t)BK7258_I2C_UNIT,   // 来自 bk7258_i2c.h
};
// L534
ret = i2c_register(&g_bk7258_i2c.dev, CONFIG_BK7258_I2C_BUS);
```

#### 3.4.2 PWM（AP）— `chip/ap/bk7258_pwm.c`

- `bk7258_pwm_initialize()`（L437）直接 `pwm_register(BK7258_PWM_DEVPATH, &g_bk7258_pwm.dev)`（L439）。
- `bk7258_pwm_start()`（L282）：把 NuttX `pwm_info_s`（频率 Hz + 16.16 定点占空比）换算为 SDK 周期/开启节拍：`period = CLK_HZ / frequency`，`on_ticks = period * duty`；SDK 对占空比取反，故传 on-ticks。
- `stop`/`shutdown` 只停本通道，不反初始化全局 driver（SDK 12 通道共享、无引用计数）。

```c
// chip/ap/bk7258_pwm.c:104-121
static const struct pwm_ops_s g_bk7258_pwm_ops =
{
  .setup = bk7258_pwm_setup, .shutdown = bk7258_pwm_shutdown,
  .start = bk7258_pwm_start, .stop = bk7258_pwm_stop, .ioctl = bk7258_pwm_ioctl,
};
// L437-439
int bk7258_pwm_initialize(void)
{ return pwm_register(BK7258_PWM_DEVPATH, &g_bk7258_pwm.dev); }
```

#### 3.4.3 GPIOE（AP，简述）— `chip/ap/bk7258_gpioe.c`

`ioexpander_ops_s`（L156）映射到 `bk_gpio_*`：`ioe_direction`→`bk_gpio_set_config`，`ioe_writepin/readpin`→`bk_gpio_set_output_value`/`bk_gpio_get_input`，中断 `ioe_attach`→`bk_gpio_register_isr`+`bk_gpio_enable_interrupt`。注意 `IOEXPANDER_OPTION_WAKEUPCFG` 在 AP 端返回 `-ENOTSUP`（唤醒源属 CP 电源域）。该驱动只返回 `ioexpander_dev_s` 对象（L803），由板级用 `gpio_lower_half()` 发布为 `/dev/gpioN`。`CONFIG_BK7258_GPIOE_NPINS` 默认 16、上限 47，且不得超 `CONFIG_IOEXPANDER_NPINS`。

### 3.5 驱动配置参数（Kconfig）

位置：`board/bk7258/chip/Kconfig`（芯片级）、`board/bk7258/Kconfig`（板级）。

| 配置项 | 行号(约) | 说明 |
|---|---|---|
| `BK7258_WDT` | L1139 | `bool default y`，`depends on ARCH_CHIP_BK7258 && !BK7258_AP_CORE`（仅 CP），`select WATCHDOG` |
| `BK7258_GPIOE` | L1470 | `select BK7258_SDK_IPC_RUNTIME`、`select IOEXPANDER`；`BK7258_GPIOE_NPINS`（`int default 16 range 1 48`） |
| `BK7258_I2C` | L1486 | `select I2C`、`select I2C_DRIVER`；`BK7258_I2C_BUS`（`range 0 1`） |
| `BK7258_I2S` | L1502 | `select AUDIO`、`select I2S`；含 `BK7258_I2S_GPIO_GROUP`/`WORKER_PRIORITY`/`QUEUE_DEPTH` |
| `BK7258_PWM` | L1533 | `depends on BK7258_AP_CORE && PWM && !PWM_MULTICHAN`；`BK7258_PWM_BUS`（`range 0 15`）、`BK7258_PWM_CHAN`（`range 0 11`） |
| `BK7258_RTC` | L1786 | `select RTC`/`RTC_DATETIME`/`RTC_DRIVER` |
| `BK7258_SARADC` | L1794 | `depends on BK7258_AP_CORE`，`select ANALOG`/`ADC`；`BK7258_SARADC_CHAN`（`range 0 15`） |
| `BK7258_SDMADC` | L1861 | `select ANALOG`/`ADC`；`BK7258_SDMADC_CHAN`（`range 1 13`） |
| `BK7258_SPI` | L1880 | `select SPI`；`BK7258_SPI_BUS`（`range 0 1`） |
| `BK7258_TIMER` | L1930 | `select TIMER`；`BK7258_TIMER_CHAN`（`range 3 5`，默认 3） |
| `BK7258_DMIC` | L2028 | `depends on BK7258_AP_CORE && BK7258_PM_CLOCK && !BK7258_MIC && !BK7258_AUD` |
| `BK7258_SARADC_SERVER` | L1220 | `depends on !BK7258_AP_CORE`，`select BK7258_SDK_IPC_RUNTIME`/`BK7258_SDK_IRQ_BRIDGE`（CP） |

> 注意：`CONFIG_IOEXPANDER_NPINS`、`CONFIG_IOEXPANDER_INT_ENABLE` 等来自 NuttX 核心 `drivers/ioexpander/Kconfig`，GPIOE 驱动依赖它们。

### 3.6 跨核通信（CP ↔ AP：RPTUN / Mailbox）

核心文件：`chip/common/bk7258_rptun.c`、`chip/common/bk7258_rptun_mbox.c`、`chip/include/bk7258_rptun.h`、`chip/include/bk7258_amp.h`；文档 `docs/bk7258-t5ai/nuttx-port/n9-rptun-source-verification.md`。

**机制**：
- 采用官方推荐 **wrapper 接入**：复用 SDK 的 public mailbox channel + AON RTC（不改 SDK），复用 NuttX RPTUN/OpenAMP/RPMsg core（不改 NuttX）；team 层只实现 shared-memory ABI、RPTUN lower-half、mailbox 延迟 worker、AP CPU0 gateway。
- 数据路径：`SDK mailbox channel / shared SRAM → BK7258 RPTUN lower-half → stock NuttX RPTUN → OpenAMP virtio → RPMsg → service`。
- **角色**：CP 是 master/resource author（`bk7258_rptun.c:192-245` 仅 CP 构造 resource table）；CP 名 `"cp"`、AP 名 `"ap"`（`CONFIG_BK7258_AP_CORE` 切换 remote 名）。
- **notify（边沿 vs 电平）**：SDK logical-channel ISR（`bk7258_rptun_mbox.c:92-129`）仅复制消息、合并 pending、唤醒 worker；`bk7258-rptun-rx` worker（L258-324）固定 AP logical CPU0，drain mailbox 后轮询 shared pending 电平状态（因 `notify()` 返回不保存丢失边沿）。
- **irq.h 映射**：`BK7258_IRQ_MAILBOX = BK7258_IRQ_FIRST + 63 = 逻辑 79`，占向量槽 [79]。

### 3.7 驱动注册流程

**AP 侧**（集中注册）：
- 入口：`chip/ap/bk7258_ap_main.c:735` 调用 **`bk7258_peripherals_initialize()`**（`chip/ap/bk7258_peripherals.c:262`）。
- 流程：取板级 binding → 若 `CONFIG_BK7258_SDK_IPC_RUNTIME` 则先 `bk7258_sdk_runtime_initialize()` → 按 `CONFIG_*` 逐一调各驱动 `bk7258_*_initialize()`（I2C/PWM/RTC/SARADC/SDMADC/TIMER/AUD/MIC 失败即致命；SPI/I2S/SDIO/CAN 为 best-effort 绑定，失败仅记日志）。

**CP 侧**（集中在 `board/bk7258/src/bk7258_platform.c`，CP 的 `board_app_initialize`）：
- `bk7258_saradc_server_initialize()`（platform.c:168）
- `bk7258_gpio_lowerhalf_initialize()`（platform.c:287/418）
- `bk7258_wdt_initialize()`（platform.c:394，注册 `/dev/watchdog0`）
- 另有 `bk7258_pm_initialize()`、`bk7258_temperature_initialize()`、`bk7258_wifi_controller_initialize()`、`bk7258_bt_controller_ipc_initialize()` 等 CP 服务端，均在释放 AP 前注册。

> ⚠️ **澄清**：`boards/t5ai_core/src/bk7258_board_bringup.c` **本身不直接注册外设驱动**——它是"板级绑定提供者"，定义 `bk7258_board_get_binding()` 返回的 `bk7258_board_binding_s`（含 mic/audio/early_initialize/devices_initialize 回调），被 `bk7258_peripherals_initialize()` 在 AP 侧调用。

### 3.8 驱动适配常见坑（面试加分项）

> 来自 11 个驱动的双轮代码审查结论（可复用到后续 SDK→NuttX 适配）。

| 缺陷模式 | 问题 | 修复模式 | 适用 |
|---|---|---|---|
| worker 线程生命周期 | shutdown 设 `running=false` 后立即关 mq，无 STOP/无 join → 悬垂 fd、双 worker 竞态 | 发 STOP → join → 再关 mq；worker 收 STOP 后自关 mq | pthread+mqueue 驱动 |
| 取消集合 | cancelbuffer 为 no-op，stop 后上层释放 buffer，worker 队列残留 → 悬垂 | 用 `cancelled[]` 数组记录被取消 apb；worker 处理前检查 | audio 类驱动 |
| ISR vs 任务 TOCTOU | ISR 读回调槽无保护，detach 持锁清空 → 调 NULL/悬垂 | ISR 临界区快照 callback/arg；detach 顺序：disable_interrupt→register_isr(NULL)→清槽 | 所有 ISR 读共享槽的驱动 |
| ISR 内 re-arm 竞态 | ISR 调 start 重装，与任务 stop 竞争 → running 与硬件发散 | `running` 作门闩；stop 先置 false 再停硬件；ISR 每次查 running | timer 类驱动 |
| SDK 命令非幂等 | `wait_cmd_response` 是消费式，sendcmd 已 pop → 重复 pop/超时 | 核实 SDK 函数语义；非幂等只在 sendcmd 消费一次 | 任何封装 SDK 队列/信号量的驱动 |
| 双配置一致性 | `CONFIG_BK7258_GPIOE_NPINS` 与 `CONFIG_IOEXPANDER_NPINS` 不一致 → 移位 UB | 编译期 `#error` 断言；移位用 `(ioe_pinset_t)1 << pin` | gpioe 等 |

---

## 4. 关键文件与目录结构梳理

### 4.1 顶层目录结构（各目录作用）

```
/home/lijian/project/open-vela/contest2026_135_yongwangzhiqian/
├── README.md                        参赛仓使用说明 + 工程拉取/提交流程
├── AGENTS.md                        项目记忆协议 + BK7258 resume 安全约束
├── contest2026_135_yongwangzhiqian.xml  本仓 repo 清单(manifest)
├── app/                             应用代码(demos / vela_claw)
├── board/                           板级适配核心(见下)
├── docs/                            BK7258-T5AI 全部技术文档(见下)
├── logs/                            AI Coding 日志 + 硬件调试归档
├── memory/                          架构决策记录 ADR-000..026
├── progress/                        当前进度 CURRENT.md + milestones/verification
├── qemu-bk7258/                     【本文未涉及】QEMU 板级
├── quickapp/                        快应用骨架
├── tests/                           仓级 C 主机测试
└── tools/                           BK7258 构建/打包/烧录/调试工具
```

### 4.2 board/bk7258/ 子结构

```
board/bk7258/
├── bootloader/     Tier-1 BL1 源码 + bl2/ MCUboot 目录 + 打包脚本
├── chip/           芯片/架构移植: cp/ ap/ common/ include/
├── boards/         具体板型: t5ai_core/ t5_board/ aidk_ai_toy/(各含 include+src)
├── bk_idk/         ARMINO SDK 静态库 bundles + manifests + sdk-profiles
├── configs/        保留 seed 配置(bl2_mcuboot / t5ai_core_cp_base / t5ai_core_ap_base / personal)
├── partitions/     Flash 分区表(bk7258/auto_partitions.csv 等)
├── scripts/        Make.defs 等构建钩子
├── src/            include/ cmake/ panels/   板级通用源码/头/CMake/Panel
└── tests/          板级 C 测试
```

### 4.3 tools/ 子结构

```
tools/bk7258/
├── bk7258_framework.py              Canonical product 解析/校验(host-only)
├── bk7258_isolated_executor.py      隔离编译合约(prepare/materialize/compile)
├── materialize_product_profiles.py  由 plan 渲染临时 role view
├── bk7258_bkpack.py / *.py          打包/信任链/AB 布局/校验
├── build_dual_image.sh              兼容适配器:编 CP app.bin + AP app1.bin
├── build_package.sh                 个人入口:BL1+BL2+CP+AP → firmware.bkpack
├── bk7258_auto_debug.sh             自动构建+稀疏烧录+冷启动采集+判读
├── setup_bk7258_sdk.sh / import_bk7258_sdk_role.sh  SDK bundle 校验/导入
└── tests/                           pytest 测试套件
```

### 4.4 docs/bk7258-t5ai/ 子结构（文档核心）

```
docs/bk7258-t5ai/
├── README.md / porting-report.md         总入口 + 详细移植报告
├── beginner-porting-guide/               新手 00–11 教程 + appendix-key-files.md
├── hardware/                             README + t5ai-core-v101-hardware-map.md + schematics/
├── bootloader/                           两家官方 bootloader 逆向综合
├── probe/                                最小裸机探针(probe.c/ld)
├── nuttx-port/                           N1–N15 各阶段 worklog + 构建/烧录 SOP
├── learning/                             学习地图(架构层/子系统心智模型)
└── jlink-swd-debug-guide.md              J-Link SWD 调试指南
```

### 4.5 最关键的 30 个文件速查表

| 路径 | 作用 | 标签 |
|---|---|---|
| `board/bk7258/bootloader/start.S` | BL1 汇编入口 + 硬化跳转 | [BL] |
| `board/bk7258/bootloader/boot_main.c` | BL1 C 主函数（分区/校验/BL2 拷贝） | [BL] |
| `board/bk7258/bootloader/boot_clock.c` | 冷启动 DPLL 上电 | [BL] |
| `board/bk7258/bootloader/boot_runtime.c` | cache/MPU/次核归一化 | [BL] |
| `board/bk7258/bootloader/bootloader.ld` | BL1 链接脚本 | [BL] |
| `board/bk7258/bootloader/Makefile` | 编译 BL1 | [BL] |
| `board/bk7258/bootloader/bl2/bk7258_bl2_main.c` | BL2 主流程 | [BL] |
| `board/bk7258/bootloader/bl2/bl2.ld` | BL2 链接脚本（SRAM） | [BL] |
| `board/bk7258/bootloader/bl2/include/mcuboot_config/mcuboot_config.h` | MCUboot 配置 | [BL] |
| `board/bk7258/bootloader/bl2/bk7258_bl2_keys.c` | 验签公钥 | [BL] |
| `board/bk7258/bootloader/README.md` | BL1/BL2 启动链权威说明 | [BL] |
| `board/bk7258/partitions/bk7258/auto_partitions.csv` | Flash 分区表（活跃） | [BL] |
| `board/bk7258/include/bk7258_partition_layout.h` | 分区布局生成头 | [BL] |
| `board/bk7258/chip/include/bk7258_memorymap.h` | 内存映射 | [BL/DRV] |
| `board/bk7258/chip/common/bk7258_rptun.c` | CP/AP 核间 RPTUN wrapper | [DRV] |
| `board/bk7258/chip/common/bk7258_rptun_mbox.c` | Mailbox 实现 | [DRV] |
| `board/bk7258/chip/common/bk7258_irq.c` | SDK IRQ bridge | [DRV] |
| `board/bk7258/chip/common/bk7258_psram.c` | 16 MiB PSRAM 识别 | [DRV] |
| `board/bk7258/chip/cp/bk7258_start.c` | CP(CPU0) 启动入口 | [DRV] |
| `board/bk7258/chip/cp/bk7258_wdt.c` | CP 看门狗 | [DRV] |
| `board/bk7258/chip/cp/bk7258_gpio_lowerhalf.c` | CP 板级 GPIO /dev/gpioN | [DRV] |
| `board/bk7258/chip/cp/bk7258_saradc_server.c` | CP SARADC mailbox 服务端 | [DRV] |
| `board/bk7258/chip/ap/bk7258_ap_main.c` | AP 主函数（调 peripherals_initialize） | [DRV] |
| `board/bk7258/chip/ap/bk7258_peripherals.c` | ★AP 外设总注册入口 | [DRV] |
| `board/bk7258/chip/ap/bk7258_i2c.c` `spi.c` `pwm.c` `saradc.c` `sdmadc.c` `gpioe.c` `rtc.c` `timer.c` `i2s.c` `dmic.c` | 各 AP 外设驱动 | [DRV] |
| `board/bk7258/chip/Kconfig` | 芯片级 Kconfig（所有 BK7258_*） | [DRV] |
| `board/bk7258/bk_idk/README.md` | SDK 版本布局与选择规则 | [BLD] |
| `board/bk7258/bk_idk/armino_as_lib/versions/<ver>/{cp,ap}` | ARMINO SDK 静态库 | [BLD] |
| `board/bk7258/src/bk7258_platform.c` | CP board_app_initialize（CP 驱动注册） | [DRV] |
| `board/bk7258/boards/t5ai_core/src/bk7258_board_bringup.c` | 板级绑定提供者 | [DRV] |
| `tools/bk7258/build_package.sh` | 打包 BL1+BL2+CP+AP → firmware.bkpack | [BLD] |
| `tools/bk7258/bk7258_auto_debug.sh` | 自动构建+稀疏烧录+判读 | [BLD] |
| `docs/bk7258-t5ai/nuttx-port/bk7258-build-flash-debug-sop.md` | 构建/烧录/调试 SOP（权威） | 文档 |

---

## 5. 构建、烧录与调试流程

### 5.1 构建前提

- 构建在 **openvela 工作区根目录**（`/home/lijian/project/open-vela`）进行，NuttX 用统一 `./build.sh <board-config-path>` 入口。
- 本仓代码通过 manifest `<linkfile>` 已软链进编译树，**无需手动 copy**。
- 需要 `arm-none-eabi-gcc`（10.3 验证）与 Python3。

### 5.2 标准构建步骤

```bash
cd /home/lijian/project/open-vela

# 1) 生成产品构建计划(host-only)
python3 tools/bk7258/bk7258_framework.py build-plan \
  --product t5_board_bringup --out /tmp/bk7258-build-plan.json

# 2) 渲染临时 role view 到 build-root/configs
python3 tools/bk7258/materialize_product_profiles.py \
  --plan /tmp/bk7258-build-plan.json \
  --seed-root board/bk7258/configs --output /tmp/bk7258-build/configs \
  --make-defs board/bk7258/scripts/Make.defs

# 3) 标准 NuttX 编译(实际产出固件)
./build.sh <board-config-path> [-j8]
# <board-config-path> 指向 board/bk7258/configs/<seed>(如 t5ai_core_cp_base / t5ai_core_ap_base / bl2_mcuboot)
```

### 5.3 编译 BL1（自研 bootloader）

```bash
cd /home/lijian/project/open-vela/contest2026_135_yongwangzhiqian/board/bk7258/bootloader
make            # 产出 bl.elf / bl.bin(逻辑) / bl_crc.bin(物理, 0x11000)
make verify     # 符号与 FAL 表检查
```

### 5.4 产物索引（构建后位于 `nuttx/bk7258-dual/`）

| 产物 | 含义 |
|---|---|
| `bl_crc.bin` | BL1 物理镜像（@0x0, 0x11000） |
| `bl2_crc.bin` / `bl2_secondary_crc.bin` | MCUboot BL2 主/备 |
| `app.bin` / `app_crc.bin` | CP 镜像（CPU0） |
| `app1.bin` / `app1_crc.bin` | AP 镜像（CPU1/2） |
| `nuttx-cp.elf` / `nuttx-ap.elf` | CP/AP 调试 ELF |
| `firmware.bkpack` | **最终下载入口**（含 BL1/BL2/CP/AP/MCUboot A/B 对） |

> ⚠️ **不要直接烧录** `vela_*.bin` 或未补齐的 `*_crc.bin`，最终入口是 `firmware.bkpack`。

### 5.5 打包（个人）

```bash
cd /home/lijian/project/open-vela/contest2026_135_yongwangzhiqian
tools/bk7258/build_package.sh \
  --product t5_board_bringup --out /tmp/bk7258-package \
  --mcuboot-key ~/.bk7258/keys/mcuboot-signing-dev.pem \
  --bl1-key ~/.bk7258/keys/bl1-manifest-dev.pem
# 产物:$OUT/firmware.bkpack(密钥不入库)
```

### 5.6 烧录

- **下载工具**：Windows 侧 `bk_loader.exe`（Beken BKFIL），经 WSL 调用。
- **日常 CP/AP 稀疏更新（推荐）**：
  ```bash
  BK7258_PRODUCT=t5_board_bringup \
    ./contest2026_135_yongwangzhiqian/tools/bk7258/bk7258_auto_debug.sh \
    --build --flash --sparse-flash --apps-only --no-console
  ```
  只写 CP/AP，保留 BL1/BL2/Manifest/LittleFS/usr_config。
- **Bootloader-only 烧录**：`bk_loader ... --mainBin-multi board/bk7258/bootloader/bl_crc.bin@0x0-0x11000`。

### 5.7 调试

- **J-Link SWD**：`JLinkGDBServerCL.exe -device Cortex-M33 -if SWD -speed 4000 -port 2331`，WSL 侧 `gdb-multiarch` 连 `localhost:2331`；可 `monitor reset halt`、断 `bk7258_hardfault_handler`、读 CFSR/HFSR 定位崩溃。
- **UART console**：波特率 460800 8N1（console UART1）；NSH 可用 `apctl status`、`uname -a`、`ps`、`ls /dev`、`bkrpmsgtest` 等。
- **冷启动判据**：有效 cold trace 必须含 `BClk`；`PASS_NSH` 仅证明 CP shell，RPTUN 需 `BRPT PASS`。

### 5.8 SDK 版本管理（要点）

| 版本 | 角色 | 状态 |
|---|---|---|
| `legacy` | cp+ap | 迁移前保留，**禁止用于构建/验证** |
| `v3.1.1.9` | cp+ap | **当前默认唯一 active 版本** |
| `v3.1.1.9-sdio4` | ap only（四线 TF） | AP 变体，CP 仍用原版 |

切换：`BK7258_SDK_BUNDLE_VERSION=legacy`（历史方式，当前规则禁止）；默认不设置即用 `v3.1.1.9`。未知版本立即报错。校验 fail-closed：manifest 必须逐字节匹配本地 bundle。

---

## 6. 面试准备速记（Cheat Sheet）

**一句话整体流程**：
> BootROM → BL1（自研，XIP@0x02000000）→ [可选 BL2：MCUboot，SRAM@0x28020000，ECDSA-P256 验签 CP+AP] → CP NuttX（CPU0）→ CP 释放 AP（CPU1/2）→ 应用。

**三个必须记住的地址**：
- BL1：`0x02000000`｜CP：`0x02010000`｜AP：`0x02150000`｜BL2：`0x024d0000`
- BL2 执行窗：`0x28020000`（SRAM）

**三个必须记住的目录**：
- `board/bk7258/bootloader/` —— bootloader（BL1 + BL2）
- `board/bk7258/chip/{cp,ap,common}/` —— 驱动与芯片适配
- `board/bk7258/bk_idk/armino_as_lib/` —— SDK 静态库集成

**驱动适配的核心范式**：
- "零寄存器访问"：NuttX lower-half 封装 armino `bk_*` HAL。
- AP 驱动在 `bk7258_peripherals_initialize()` 集中注册；CP 驱动在 `bk7258_platform.c` 注册。
- 跨核用 RPTUN/RPMsg（SDK mailbox + 共享 SRAM）。

**易考点**：
- 冷复位为什么不止"读 header + branch"？（cache/MPU/WDT/次核/中断状态都要清理）
- 为什么 I2C/PWM 驱动要 `CONFIG_BK7258_AP_CORE` 守卫？（符号只在 AP `libdriver.a` 中）
- 双核启动由谁释放 AP？（CP 经 sys 寄存器 reset CPU1，不在 bootloader 链路）
- 安全启动现状？（开发根，未启用 OTP/eFuse）

---

## 附：引用源码路径索引（按章节）

| 章节 | 主要引用路径 |
|---|---|
| §2 BL1 | `board/bk7258/bootloader/{start.S, boot_main.c, boot_clock.c, boot_runtime.c, bootloader.ld, Makefile, bk7258_bl1_pack.py}` |
| §2 BL2 | `board/bk7258/bootloader/bl2/{bk7258_bl2_main.c, bk7258_bl2_flash_map.c, bk7258_bl2_keys.c, bl2_start.S, bl2.ld, include/mcuboot_config/mcuboot_config.h}` |
| §2 分区 | `board/bk7258/partitions/bk7258/auto_partitions.csv`、`board/bk7258/include/bk7258_partition_layout.h` |
| §3 芯片 | `board/bk7258/chip/{cp,ap,common}/`、`board/bk7258/chip/Kconfig` |
| §3 SDK | `board/bk7258/bk_idk/{README.md, armino_as_lib/versions/<ver>/{cp,ap}, sdk-bundles.cmake}` |
| §3 驱动 | `board/bk7258/chip/ap/{bk7258_i2c.c, bk7258_pwm.c, bk7258_gpioe.c, bk7258_saradc.c, bk7258_sdmadc.c, bk7258_rtc.c, bk7258_timer.c, bk7258_i2s.c, bk7258_dmic.c}`、`board/bk7258/chip/cp/{bk7258_wdt.c, bk7258_gpio_lowerhalf.c, bk7258_saradc_server.c}` |
| §3 注册 | `board/bk7258/chip/ap/bk7258_peripherals.c`、`board/bk7258/src/bk7258_platform.c`、`board/bk7258/boards/t5ai_core/src/bk7258_board_bringup.c` |
| §3 跨核 | `board/bk7258/chip/common/{bk7258_rptun.c, bk7258_rptun_mbox.c}`、`chip/include/{bk7258_rptun.h, bk7258_amp.h}` |
| §4 结构 | 全仓目录树 |
| §5 构建 | `tools/bk7258/{bk7258_framework.py, materialize_product_profiles.py, build_package.sh, bk7258_auto_debug.sh}`、`docs/bk7258-t5ai/nuttx-port/bk7258-build-flash-debug-sop.md` |

> 所有路径均可从 `/home/lijian/project/open-vela/contest2026_135_yongwangzhiqian/` 起定位。行号引用按探查时文件实际行号，可能因版本演进偏移，请以 `文件:函数名` 为主锚点。

---

*文档生成说明：本文基于 `contest2026_135_yongwangzhiqian/` 仓库静态探查整理，未涉及 `qemu-bk7258/` 与 `nuttx/.worktrees/` 中间产物。部分行号/容量信息来自文档推断，已在正文以 ⚠️ 标注待核实项。*
